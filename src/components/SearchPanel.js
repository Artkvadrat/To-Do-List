import React from 'react';
import './SearchPanel.css';

const SearchPanel = () => {
    return (
        <input placeholder="type to search" className="searchLine" />
    )
};

export default SearchPanel;